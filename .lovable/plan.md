

## Тонкая корректировка позиции фото Сарпера

На скриншоте видно, что при `-25%/-25%` лицо ушло слишком высоко (лоб обрезан) и немного влево. Нужна корректировка:

### Изменения

В `src/components/TestimonialsSection.tsx`, строка с `img`:
- `marginLeft`: с `"-25%"` на `"-20%"` -- чуть меньше сдвиг влево, лицо ближе к центру по горизонтали
- `marginTop`: с `"-25%"` на `"-15%"` -- меньше сдвиг вверх, чтобы лоб не обрезался

Это должно поставить лицо ровно по центру круга.
